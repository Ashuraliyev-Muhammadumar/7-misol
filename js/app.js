function removeStringSpaces(){
    let namesOf = ["hello", 45, -3, null, true, undefined, false,{age: 40} ] ;
    let math = Math.floor(Math.random() * 15);
    console.log(namesOf[math]);
}
removeStringSpaces()